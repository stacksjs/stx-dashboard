<script>
const label = ''
const value = 0
const unit = ''
const variant = ''
</script>

<div class="bg-slate-800 rounded-lg p-5 border border-slate-700">
  <span class="block text-xs text-slate-400 uppercase tracking-wider font-medium">{{ label }}</span>
  <span class="block text-2xl font-bold mt-2 {{ variant === 'failed' ? 'text-red-400' : 'text-slate-100' }}">{{ value }}{{ unit ? ' ' + unit : '' }}</span>
</div>
